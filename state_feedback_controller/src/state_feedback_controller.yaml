state_feedback_controller:
  command_interfaces: {
    type: string_array,
    default_value: [],
    description: "Name of the interface used for command.",
    read_only: true,
  }
  state_interfaces: {
    type: string_array,
    default_value: [],
    description: "Name of the interface used for state.",
    read_only: true,
  }
  state_feedback_gain: {
    type: double_array,
    default_value: [],
    description: "State feedback gain matrix. Data must be stored as column major.",
  }